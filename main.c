
    // // Lê o resto da linha (os nomes) como uma string
    // fgets(restante, sizeof(restante), stdin);

    // // Utiliza strtok para dividir a string de nomes em até dois nomes
    // char *nome1 = strtok(restante, " \n");
    // char *nome2 = strtok(NULL, " \n");
    // char *nome3 = strtok(NULL, " \n");

    // if(nome1 && nome2 && nome3 != NULL){
    //     printf("Nome 1: %s\n", nome1);
    //     printf("Nome 2: %s\n", nome2);
    //     printf("Nome 3: %s\n", nome3);
    // }
    // else if(nome1 && nome2 != NULL){
    //     printf("Nome 1: %s\n", nome1);
    //     printf("Nome 2: %s\n", nome2);
    // }

